- @title = "#{@move.name_was} Edit"

- content_for :header do
  .background-gradient
  .header-container
    .container
      %h1.page-heading
        = link_to @move.name_was, @move
        Edit

.dashboard-container= render 'form', move: @move
