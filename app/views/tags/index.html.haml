%h1 Tags

= link_to 'Moves', moves_path, class: 'btn btn-default btn-xs'

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th # of Moves
      %th{ colspan: 2 }

  %tbody
    - @tags.each do |tag|
      %tr
        %td= link_to tag.name, tag
        %td= tag.moves.count
        %td= link_to 'Edit', edit_tag_path(tag)
        %td= link_to 'Destroy', tag, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Tag', new_tag_path
